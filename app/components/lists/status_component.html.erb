<span class="inline-flex items-center leading-5">
  <%= icon(
    name: icon_name,
    class: "h-4 w-4 mr-3 #{status_color} #{'spin' if show_spinner?}"
  ) %>
  <span class="capitalize"><%= @list.status %></span>
</span>
